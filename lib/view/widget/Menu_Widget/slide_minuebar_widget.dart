import 'package:flutter/material.dart';

class SlideMenuBarPage extends StatelessWidget {
  const SlideMenuBarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueGrey,
    );
  }
}